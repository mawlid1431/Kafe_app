# Copy to .env.local and fill in values (never commit .env.local)

# Expo / EAS project ID — from expo.dev or app.json → extra.eas.projectId
EAS_PROJECT_ID=

# Use Bun when Expo CLI installs packages
EXPO_USE_BUN=1
